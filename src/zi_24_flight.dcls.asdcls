@EndUserText.label: 'Flight'
@MappingRole: true
define role ZI_24_FLIGHT {
    grant select on ZI_24_FLIGHT where
    (CarrierID) = aspect pfcg_auth (S_CARRID, CARRID, ACTVT = '03');
                        
}